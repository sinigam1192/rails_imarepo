<h1>ユーザー管理画面</h1>
<h2><%= link_to("#{@user.name}", "/users/#{@user.id}/edit") %>さんのページです</h2>
<% if @current_user.id == @user.id %>
<p><%= link_to "レポートを作成する", "/work/report/new" %></p>
<p><%= link_to "グループを作成する", "/work/company/new" %></p>
<p><%= link_to "マイリスト", "/users/#{@user.id}/mylist" ,class:"btn btn-primary"%></p>
<% end %>
